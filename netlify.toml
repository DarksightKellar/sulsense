[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: serve index.html for client-side routes (static assets are served from dist first)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
